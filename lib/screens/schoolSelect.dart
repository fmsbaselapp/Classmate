import 'package:flutter/material.dart';

class SchoolSelectScreen extends StatelessWidget {
  const SchoolSelectScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('1'),
    );
  }
}